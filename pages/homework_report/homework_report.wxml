<view class="container">

  <view class="layout">
    <view class="homework_title">课后作业名</view>

    <view class="flex">
      <view class="shu1"></view>
      <view class="v1">您的得分：</view>
      <view class="score">{{report.chart.get_score}}</view>
      <view style="font-size:26rpx;color:rgba(32,32,32,1);margin-left: 22rpx;margin-top: 69rpx;">分</view>
      <view class="sum">总分：100分</view>
    </view>
    
    <view class="flex">
      <view class="shu2"></view>
      <view class="submit_time">交卷时间：2020-03-08 20:22</view>
      <view class="shu2"></view>
      <view class="submit_time">用时：15分35秒</view>
    </view>

    <view class="heng"></view>

    <view class="ques_type" wx:if="{{ques_type1 == 1}}">单选题</view>
    <view class="warp" >
      <view class="for" wx:for="{{report.option}}" wx:key="index">
        <view wx:if="{{item.type == 1}}">
          <view class="r-a-w" wx:if="{{item.status == 1  }}" style="background:rgba(3,182,131,1);">{{index +1}}</view>
          <view class="r-a-w" wx:if="{{item.status == 3  }}" style="background:rgba(248,107,107,1);">{{index +1}}</view>
        </view>
      </view>
    </view>

    <view class="ques_type" wx:if="{{ques_type2 == 2}}">多选题</view>
    
    <view class="warp" >
      <view class="for" wx:for="{{report.option}}" wx:key="index">
        <view wx:if="{{item.type == 3}}">
          <view class="r-a-w" wx:if="{{item.status == 1}}" style="background:rgba(3,182,131,1);">{{index +1}}</view>
          <view class="r-a-w" wx:if="{{item.status == 3}}" style="background:rgba(248,107,107,1);">{{index +1}}</view>
          <view wx:if="{{item.status == 2}}">
            <view class="ques_num">{{index +1}}</view>
            <image class="r-a-w_img" src="../../images/r-a-w.png"></image>
          </view>
        </view>
      </view>
    </view>

    <view class="ques_type" wx:if="{{ques_type3 == 3}}">填空题</view>
    <view class="warp" >
      <view class="for" wx:for="{{report.option}}" wx:key="index">
        <view wx:if="{{item.type == 3}}">
          <view class="r-a-w" wx:if="{{item.status == 1  }}" style="background:rgba(3,182,131,1);">{{index +1}}</view>
          <view class="r-a-w" wx:if="{{item.status == 3  }}" style="background:rgba(248,107,107,1);">{{index +1}}</view>
        </view>
      </view>
    </view>

    <view class="ques_type" wx:if="{{ques_type4 == 4}}">简答题</view>
    <view class="warp" >
      <view class="for" wx:for="{{report.option}}" wx:key="index">
        <view wx:if="{{item.type == 4}}">
          <view class="r-a-w" wx:if="{{item.status == 1}}" style="background:rgba(3,182,131,1);">{{index +1}}</view>
          <view class="r-a-w" wx:if="{{item.status == 3}}" style="background:rgba(248,107,107,1);">{{index +1}}</view>
          <view wx:if="{{item.status == 2}}">
            <view class="ques_num">{{index +1}}</view>
            <image class="r-a-w_img" src="../../images/r-a-w.png"></image>
          </view>
        </view>
      </view>
    </view>

    <view class="ques_type" wx:if="{{ques_type5 == 5}}">组合题</view>
    <view class="warp" >
      <view class="for" wx:for="{{report.option}}" wx:key="index">
        <view wx:if="{{item.type == 5}}">
          <view class="r-a-w" wx:if="{{item.status == 1}}" style="background:rgba(3,182,131,1);">{{index +1}}</view>
          <view class="r-a-w" wx:if="{{item.status == 3}}" style="background:rgba(248,107,107,1);">{{index +1}}</view>
          <view wx:if="{{item.status == 2}}">
            <view class="ques_num">{{index +1}}</view>
            <image class="r-a-w_img" src="../../images/r-a-w.png"></image>
          </view>
        </view>
      </view>
    </view>

  
  </view>










<!-- container -->
</view>
